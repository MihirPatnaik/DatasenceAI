// src/smartsocial/components/ui/toaster.tsx

/*import React from "react";
import { useStandaloneToast } from "./use-toast";
import { ToastProps } from "./toast";*/


export function Toaster() {
    // This component is now just a placeholder since the actual toasts
  // are rendered through portals in the ToastProvider
  
  //const { toasts } = useStandaloneToast();

   return null;
}

    //<div className="fixed bottom-4 right-4 space-y-2 z-50">
      //{toasts.map((toast) => (
        //<div
          //key={toast.id}
          //className={[
            //"p-4 rounded-lg shadow text-white transform transition-all duration-300 max-w-sm",
            //toast.variant === "destructive" ? "bg-red-500" : "bg-gray-800",
            //"opacity-100 translate-y-0",
          //].join(" ")}
        //>
          //{toast.title && <div className="font-bold">{toast.title}</div>}
          //{toast.description && (
            //<div className="mt-0.5 text-sm">{toast.description}</div>
          //)}
          //{toast.action && <div className="mt-2">{toast.action}</div>}
        //</div>
      //))}
    //</div>
  //);
