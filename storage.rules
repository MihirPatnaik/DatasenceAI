rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // ✅ Public read for posts
    match /posts/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
